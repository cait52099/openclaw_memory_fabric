# OCMF — OpenClaw Memory Fabric

OCMF is a cross-host memory layer for AI developer tools.

It gives Claude and Codex a shared, traceable memory for project decisions, constraints, preferences, and execution context — so you do not have to repeatedly restate the same information across tools.

## What OCMF does
- Shared memory across Claude and Codex
- Provenance-aware recall (`From Claude`, `From Codex`)
- Unified CLI for install / remember / recall
- Event-backed memory model with auditable history
- Low-friction developer workflow via the `ocmaf` command

## Current status
- Trusted user journey: **passed (current environment)**
- Global install: **available**
- Quickstart: **verified**
- Formal PyPI release: **not published yet**

> Current boundary:
> - `CURRENT_ENV_STABLE = YES`
> - `ROOT_CAUSE_IDENTIFIED = NO`
